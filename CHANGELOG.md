# CHANGELOG

> [English version →](CHANGELOG_EN.md)

本文记录薪跳 PayDance 的正式版本变化，也是 GitHub Release 正文的来源。

构建产物与校验文件见 [GitHub Releases](https://github.com/MrBaoboer/PayDance/releases)。

## Unreleased

- **EXE 文件属性补全**：便携 EXE 现在带发行者、说明、版权和许可字段，在 Windows 文件属性面板可以直接看到。

## 已发布

### v0.9.9

- **迷你模式不再占用任务栏**：进入迷你悬浮模式时主窗口撤下任务栏按钮，退出时恢复；从托盘隐藏再显示也会重新撤下。托盘图标始终是找回窗口的入口。
- **货币符号可自定义**：设置中心新增「货币符号」，填入想要的符号即可（$、€、£、₩、HK$ 等），留空则不显示，输入框右侧用 `88.68` 实时预览。默认仍是 ¥，老用户升级后保持不变。主看板、今日预计、薪资说明、迷你悬浮窗口和薪资输入框同步生效，屏幕阅读器读出的金额也带同样的符号。不做汇率换算（[#30](https://github.com/MrBaoboer/PayDance/issues/30)）。
- **修复窗口跑到屏幕外找不回**：副屏拔掉后窗口不会被拉回。窗口恢复依赖的 `available_monitors` / `primary_monitor` 缺少权限，显示器信息始终为空，可见区域判断因此永远返回“是”。现已补齐权限，并新增一条测试：前端调用的每个窗口命令都必须有对应授权，否则构建失败。
- **修复主窗口被销毁后进程变成僵尸**：启动早期用 Alt+F4 或任务栏右键关闭窗口，会留下一个看不见、托盘点了没反应、只能用任务管理器结束的进程。标题栏的「关闭到托盘」按钮改为真正的隐藏，关闭拦截器提前到启动的第一步注册，主窗口一旦真被销毁则干净退出。
- **托盘菜单跟随语言**：托盘菜单与悬停提示此前固定以中文启动，只有手动切一次语言才会变；现在启动时恢复或按系统语言自动探测的结果也会同步到托盘。
- **迷你透明度面板跟随语言**：该面板是独立窗口，从不读取设置文件，此前永远显示中文；现在由主窗口在打开时下发当前语言。
- **无障碍修复**：主看板按钮与迷你悬浮窗口的可访问名带上实时金额，此前今日入账对屏幕阅读器完全不可见。设置里的开关补回键盘焦点框，此前被一条全局 `outline: none` 抹掉。
- **尊重系统的「减少动态效果」**：常驻置顶窗口上的进度条高光是无限循环动画，此前不受该设置影响，现已全局降级。
- **设置保存缺口**：金额变换方式、界面语言和货币符号此前没有自己的写盘触发器，进程异常结束就会回滚；现已各自触发保存。
- **英文界面标点**：关于区「版本」「作者」后的分隔符此前硬编码为中文全角冒号，现改为随语言切换。

### v0.9.8

- **修复窗口在全屏游戏后消失**：Windows 会把最小化的窗口挪到 `-32000, -32000` 这个哨兵坐标，此前会被当作真实位置写进 `salary-settings.json`，窗口因此停在屏幕外。移动事件、位置采集和写盘三处现已拒绝该坐标，被旧版本写坏的坐标会在下次启动读取设置时自动清除。
- **托盘和二次启动一定能找回窗口**：`show()` 在 Windows 上不会取消最小化，被全屏游戏最小化的窗口此前唤不回来。托盘菜单、托盘左键单击和重复运行程序现在统一先取消最小化，并校验窗口是否落在可见的显示器工作区内；已经部分可见的窗口保持用户自己的摆放。
- **最小化不再影响已保存的窗口尺寸**：最小化时被系统压缩的客户区读数和零尺寸读数都不会写盘。
- **设置文件容错加固**：窗口尺寸拒绝 `NaN`、`Infinity` 和非数字内容并限制上限，坐标拒绝非有限值与异常量级；启动阶段应用窗口模式失败不再中断后续初始化，计时器、托盘菜单和退出保存始终会注册。
- **窗口配置版本门禁护栏**：窗口尺寸的兼容判断与实际写盘的 `settingsVersion` 是两个不同的计数器，上调前者会重置所有用户的窗口尺寸。现已补充说明并加上顺序断言。
- **回归测试**：补齐最小化哨兵坐标、损坏尺寸、离屏恢复与托盘取消最小化的覆盖。
- **官网演示模式**：Web Preview 首次打开直接进入完整工资看板，使用中性的演示薪资、工作时间和偏好，并默认选中周一至周日；已有浏览器设置不会被覆盖。
- **向导随时可重开**：Web Preview 设置页新增“首次启动向导”入口，可随时重新体验并调整现有三步配置流程；Windows 桌面版首次启动行为保持不变。

### v0.9.7

- **语言优先首次配置**：首次启动向导改为“使用偏好 → 薪资模式 → 工作时间”，让外语用户第一屏即可切换语言，再继续填写薪资和工作日配置。
- **中英文独立官网入口**：中文保留 `/PayDance/`，英文使用 `/PayDance/en/`；两页分别提供标题、描述、Canonical、Open Graph 和 JSON-LD，并通过双向 `hreflang` 与自动生成的 Sitemap 建立搜索入口。结构化数据版本与 `dateModified` 在构建时自动注入。
- **官网刷新稳定性**：Web Preview 在首帧同步恢复已保存主题，并在设置初始化完成前暂停颜色过渡，避免刷新时短暂出现浅色页面与黑色控件混合的画面。
- **依赖与 CI 基线更新**：前端依赖升级到当前兼容最新版；CI、CodeQL 与发布流程切换至 Node 24 和 Windows 2025，Pages 产物读取升级至 `actions/download-artifact` 8.0.1。
- **推送提速**：日常推送只在本地执行元数据、格式、代码规范和单元测试，耗时较长的构建、浏览器 QA、Rust 检查和安全审计交给 GitHub CI；正式发布继续通过 `npm run verify:release` 执行完整检查。
- **维护基础升级**：Renovate 改为立即检查、无限并发 PR 和人工合并评估；Web Preview 新增固定环境的像素差异门禁；Windows EXE 冒烟会输出窗口响应与单实例 JSON 证据。
- **架构收敛**：Rust 托盘与便携更新、Web Preview 样式、设置页仓库信息均按职责拆分；网页构建只注入版本号，不再包含完整 `package.json`。
- **贡献者导航**：新增中英文架构修改地图，并要求新手任务明确用户可见结果、修改前证据、验收标准和验证命令。

### v0.9.6

- **首次体验修复**：移动端首次向导 Footer 始终可见；Web Preview 每次访问默认回到完整看板，不再恢复上次迷你模式。
- **设置自修复**：单项损坏时只恢复该项或最小关联组，其余有效设置保持不变；修复结果会静默写回，不再显示长期损坏提示或自动打开设置中心。
- **平台边界**：Web Preview 使用独立的存储、更新器和外链适配器，浏览器构建不再引入 Tauri 平台模块。
- **Rust 回归测试**：覆盖更新版本目录清洗、Windows EXE 识别、带空格路径参数和安装目录写入探测。
- **发布安全**：新增 CodeQL、Windows EXE 启动与单实例冒烟、SPDX SBOM；所有 GitHub Actions 固定到完整 Commit SHA。
- **官网 SEO**：分享图改为以产品 Logo 为主体，并从约 905 KB 压缩至约 45 KB；补充 Windows 桌面实时工资看板标题并修正结构化数据分类。

### v0.9.5

- **主窗口与迷你窗口位置隔离**：两个模式分别保存和恢复坐标，移动迷你窗口不再覆盖完整窗口位置。
- **多显示器与 DPI 恢复修复**：窗口位置统一使用物理像素坐标，按真实主屏、各显示器工作区和缩放因子判断可见范围，支持左右副屏、不同缩放比例、任务栏区域与显示器断开回退。
- **模式切换竞态修复**：切换前捕获当前模式位置，调整窗口尺寸后恢复目标模式的位置快照，避免尺寸变化事件污染另一套坐标。
- **回归保障**：新增窗口模式切换、独立位置、150% DPI、副屏负坐标和显示器断开场景测试。

### v0.9.4

- **桌面可靠性收口**：计时器改为混合时钟策略，长时间休眠或系统时间大幅前跳后会重新按真实墙上时间校准，同时继续避免金额倒退；窗口恢复会校验显示器可见区域。
- **本地设置更可信**：配置迁移升级为显式版本链，未来版本配置会安全隔离；设置写盘失败会在设置面板展示轻量提示，不再只进入 console。
- **更新与发布链路加固**：更新器区分开发配置缺失、正式版签名失败、清单异常和网络失败；Release dry-run 增加本次构建产物 smoke 报告，发布后 smoke 不再把 dry-run 误判为公开 Release 验证。
- **Web Preview 与可访问性**：Web Preview QA 接入 CI 门禁；语言切换同步 HTML `lang`；更新角标改为语义按钮。

### v0.9.3

- **许可证架构重组**：代码许可切换至 AGPL-3.0-only；新增 AGPL §7 附加条款（`ADDITIONAL_TERMS.md`，含署名保留、修改标注、反冒充、商标保留）；重写商标政策（`TRADEMARK.md`）；新增品牌资产许可（`BRAND-ASSETS.md`）、项目说明（`NOTICE.md`）、第三方声明（`THIRD_PARTY_NOTICES.md`）、贡献者许可协议（`CLA.md`）；文档许可从 CC BY 4.0 切换至 CC BY-SA 4.0；README 许可证章节重写为四段式（代码/文档/品牌/商业授权）。
- **砍掉 NSIS 安装包**：移除 `bundle.targets: ["nsis"]`，Release 仅发布便携版 EXE + SHA256 + `.sig` 签名。`latest.json` 更新清单指向便携版直链。清理 README、ROADMAP、CHANGELOG 等全部文档中的 NSIS/安装包表述。
- **CI 修复**：gitleaks 从失效的 `gitleaks/gitleaks-action@v2` 切换至 `choco install gitleaks` 命令行调用，解决 Windows runner 二进制 404 与 API 不兼容问题。

### v0.9.2

- **分享卡片补全**：`index.html` 新增 `og:image:alt`、`twitter:image:alt`，JSON-LD 结构化数据补充 `softwareVersion`、`downloadUrl`、`license`、`isAccessibleForFree`、`dateModified` 字段。
- **供应链安全加固**：CI 新增 gitleaks（Git 历史密钥扫描）、`cargo audit`（Rust 依赖漏洞）和 `cargo deny check`（许可证合规与来源校验）三个检查步骤。
- **治理文档体系**：新增或重写 `SECURITY.md`、`CONTRIBUTING.md`、`TRADEMARK.md`、`ROADMAP.md`、`SUPPORT.md` 及其英文版，共 10 个文件，均以中文为主版本并含英文双向链接。
- **README 刷新**：中英文核心特性均加入"中英文双语界面""静默自动更新"等描述，反映 v0.9.0 以来实际能力。
- **官网语言切换器优化**：从右下角移至右上角 Topbar 版本号左侧，暗色模式下边框、背景与文字颜色适配。
- **发布流程修复**：移除偶发 404 失败的 `attest-build-provenance` 步骤，Release workflow 可靠性提升。

### v0.9.1

- **更新系统闭环**：生成真实 Tauri updater 签名密钥对，`tauri.conf.json` 写入公钥，Release workflow 构建便携版并生成 `.sig` 签名文件与 `latest.json` 更新清单，实现完整自动更新链路。
- **更新角标状态机**：角标新增下载中（旋转 Loader）和失败（红色 AlertCircle + 错误信息 + 点击重试）两个交互状态，解决点击后无反馈的问题。
- **退出前状态保存**：托盘"退出"改为先向前端发射 `before-app-exit` 事件，前端 flush 持久化队列后再退出，避免防抖窗口期内丢失最后修改。
- **系统托盘国际化**：前端语言切换后向 Rust 发 `locale-changed` 事件，Rust 重建托盘菜单与 tooltip，中英文完整覆盖。
- **首次启动向导精简**：第三步移除"窗口始终置顶"和"进入迷你悬浮模式"两个开关，为语言和主题选项添加表头标签，面板高度与前两步保持一致。
- **窗口位置持久化**：主窗口位置在拖拽后自动保存，启动时恢复；多显示器断开后安全回退，DPI 变化不丢失坐标。
- **Web Preview 语言切换器**：官网右下角新增固定定位的语言切换按钮（中 / EN），一屏切换中英文浏览体验。
- **Web Preview 发布门禁**：部署改为 `workflow_run`（CI 通过后才触发），避免测试/构建失败导致不完整的网站被部署。
- **发布后冒烟 workflow**：新增 `post-release-smoke.yml`，Release 发布后自动验证 latest release 指向正确、README 下载链接可达、`latest.json` 存在。
- **补齐 `tauri-plugin-process`**：Rust 侧加入 process 插件初始化与 `process:allow-restart` 权限，修复更新后自动重启断链问题。
- **治理文档**：新增 `SECURITY.md`（安全报告渠道与流程）、`CONTRIBUTING.md`（开发环境、产品边界、PR 规范）、`TRADEMARK.md`（品牌保护与衍生命名规则）。
- **代码卫生**：`SettingsPanel` GitHub 按钮 `aria-label` i18n 化，`repository-metadata.test` 与 `window-chrome.test` 测试断言随结构重构更新。

### v0.9.0

- **多语言支持**：新增自建的轻量 i18n 基础设施，支持简体中文与英文切换。设置中心新增语言选择器，所有 UI 文案、状态提示、校验错误均实现双语覆盖。
- **许可证更新**：代码许可更换为 GPL-3.0-only，明确商标保护声明（"薪跳""PayDance" 为作者商标），文档采用 CC-BY-4.0。新增英文 README，原中文 README 保留为 README_CN.md。
- **产品官网 SEO**：Web Preview 首页新增 Open Graph / Twitter Card / JSON-LD 结构化数据标签，补充 `robots.txt` 与 `sitemap.xml`，改善社交媒体分享预览与搜索引擎收录。
- **自动更新机制**：接入 `tauri-plugin-updater`，应用启动时后台静默检查 GitHub Releases 最新版本。检测到新版本时，设置中心底部版本号旁出现橙色小箭头图标，点击即可下载安装并自动重启。
- **设置中心改进**：修复"检查更新"按钮无样式问题，改为静默后台检查 + 版本号旁微小角标。
- **迷你悬浮窗口**：aria-label 实现双语化，右键交互保持原有一致体验。
- **代码质量**：重构 `WindowTitlebar` 状态判断（字符串比较 → `isWorkingStatus` 布尔 prop），`useAppUpdater` 增加 try/finally 防护，`UpdaterStatus` 新增 `unavailable` 状态统一 Web/Desktop 行为。
- **工程自动化**：新增 Renovate 配置，npm + Cargo 依赖自动分组更新，补丁版本自动合并。
- **依赖更新**：npm 与 Rust 锁文件同步到当前可用版本。

### v0.8.15

- Release Windows 附件改为版本化命名，发布 `pay-dance-v0.8.15-windows-x64.exe` 与对应 SHA256 文件，降低用户本地下载文件混淆。
- README Windows 下载链接同步指向版本化 Release asset，网页端下载入口共享同一版本化下载地址。
- Web Preview 主入口拆分为页面、顶部品牌、首屏文案、操作按钮、软件展示区、迷你透明度面板和页脚组件，保留现有视觉方向不变。
- Web / Desktop 构建改为构建期选择单一运行入口，并新增构建边界断言，防止互相产出对方的孤儿 chunk。
- npm 与 Rust 锁文件依赖同步更新到当前可用补丁版本。

### v0.8.14

- Web Preview 撤回 v0.8.13 的斜向细线和轨道装饰，重构为“静默钛黑 / 高端硬件橱窗”视觉方向，以材质、层次、空间和低频光感建立高级感。
- Web Preview 暗色模式改用钛黑基底、柔和光幕、展示台材质层和更克制的窗口浮起阴影，避免背景与软件窗口混在一起。
- Web Preview 浅色模式同步改为更干净的暖白材质层，保留原有区隔感，不再引入明显装饰线。
- 网页端新增 Favicon，使用产品 Logo，并通过 GitHub Pages base 路径正确加载。
- Web Preview QA 断言同步更新，禁止线条堆叠式装饰，并检查暗色材质舞台、Favicon、移动端标签与副标题稳定性。

### v0.8.13

- Web Preview 深色模式重做页面背景层次，在软件展示区背后加入克制的琥珀光场、细线轨道和更明确的窗口承托阴影，拉开页面背景与软件窗口的视觉距离。
- Web Preview 浅色模式同步补充低透明度装饰线场，保持现有干净区隔，同时让浅深色两套背景语言更一致。
- Web Preview 桌面端副标题下移，减少与主标题之间的拥挤感；移动端继续保留前一版已优化的轻量副标题和底部标签间距。
- Web Preview 拆分状态逻辑与三枚底部标签组件，主橱窗组件更聚焦布局表达，并新增 QA 断言覆盖移动端标签重叠、副标题尺寸和深色舞台装饰。

### v0.8.12

- Release 桌面端构建改为显式 `build:desktop`，Tauri 打包前会锁定桌面目标，确保 `pay-dance.exe` 加载完整 Windows 桌面端界面。
- 新增构建产物目标断言：桌面构建必须加载 `DesktopApp`，Web Preview 构建必须加载 `WebPreviewApp`，防止两个入口在发布链路中串线。
- Web Preview 首屏左右文案区与软件展示区间距收窄，整体靠拢但不挤压软件展示窗口。
- README 顶部下载入口改为简洁文字按钮，并按“产品简介 / 界面体验 / 核心特性 / 快速下载 / 技术架构 / 开发者指南 / 隐私声明 / 作者与许可”重排章节。

### v0.8.11

- Web Preview 首屏按新参考方向重排：左侧文案和按钮区节奏更集中，三枚核心标签移到跨首屏底部信息条，保持“毫秒级更新 / 安心专注 / 隐私优先”顺序不变。
- 三枚核心标签采用图标在左、主副文案在右的横向组合，并继续通过缩放保持窄视口同排展示。
- Web Preview 背景装饰补充克制的轨道线与低透明度环境光，浅色保持干净，深色保持清爽黑色层次。
- Windows 下载与 GitHub 按钮继续锁定图标、文字的几何垂直居中校验。
- 产品定位统一为“桌面实时工资看板”，全仓清理旧定位词，并同步 Tauri 描述、托盘提示、README、PRODUCT / DESIGN 与测试断言。
- Web Preview 中文字体子集同步刷新，覆盖“桌面实时工资看板”等新增字形。

### v0.8.10

- Web Preview 副标题收敛为单行“具象化你的劳动价值，专注工作，也看见回报”，保留既有左侧文案结构与视觉重心。
- 三枚核心标签改为无边框、无背景的图标加文案组合，继续使用当前图标，并保持极窄视口下同排缩放。
- Windows 下载与 GitHub 按钮补齐文字基线微调，QA 脚本同步校验图标和文案的垂直居中偏差。
- Web Preview 主题切换边缘改为内描边承载，深色舞台层次和装饰光感进一步收敛，减少浅深色切换时的白边闪烁。
- Web Preview 中文字体子集同步刷新，覆盖本轮首屏橱窗文案并继续保持轻量体积。
- QA 流程新增浅深色连续切换检查，固化“本地服务 + 内置 Playwright + 多视口截图 + DOM/console 双校验”的发布前验证标准。

### v0.8.9

- Web Preview 补齐新版中文文案的本地字体子集，避免新增字符回退到系统字体导致标题、按钮和标签观感不一致。
- 三枚核心标签改为单行网格布局，移动端和极窄窗口下按比例缩小，保持“毫秒级更新 / 安心专注 / 隐私优先”始终水平并列。
- Windows 下载与 GitHub 按钮补齐图标垂直居中约束，QA 脚本新增按钮几何居中和标签同排校验。
- 设置中心底部版权信息略向左微调，与 GitHub 按钮保持更稳定的视觉居中。
- README 与产品文档继续收敛为产品橱窗表达，删除旧受众表述、跨平台叙事、首屏冗余句子和过重的工程细节。
- Web Preview QA 脚本延长冷启动导航超时，继续固化“本地服务 + 内置 Playwright + 多视口截图 + DOM/console 双校验”的标准流程。

### v0.8.8

- Web Preview 副标题改为“具象化你的劳动价值 / 专注工作，也看见回报”，首屏价值表达更直接、更贴近产品橱窗定位。
- Windows 下载入口改为直连 `pay-dance.exe`，并使用 Windows 11 四宫格图标与下载图标组合，降低首次下载路径成本。
- 三枚核心标签更新为“毫秒级更新 / 安心专注 / 隐私优先”，补充匹配的装饰性图标，文案与卡片内容统一居中。
- Web Preview 深色模式补充更克制的暖橙轨道与暗色背景层次，保留软件展示区既有结构与交互不变。
- 全仓作者署名统一更正为 `Mr.Baoboer`，包括应用元信息、README、许可证、Cargo 与历史记录文本。

### v0.8.7

- Web Preview 版本展示恢复为轻量的 `Web Preview · 当前版本` 文本表达，不再使用居中大胶囊；版本号仍绑定应用 `appVersion`，随正式版本自动变化。
- 首屏左右两区重新拉开距离，标题字号与橙色强调行收敛，副标题改为更紧凑的单行文案并下移，避免文案压过右侧软件展示。
- 中等宽度窗口继续保留压缩双栏，较窄窗口再切换为单列，减少软件预览被推到首屏之外的情况。
- 浅色模式左上角品牌区改为裸 Logo 展示，保留浅色背景光晕和三枚核心优势卡片不变。

### v0.8.6

- Web Preview 首屏继续重构：品牌区与版本号收敛为更居中的顶部组合，主标题改为“看见每一秒的 / 收入跳动”两行宽度对齐，副标题、下载入口和三枚核心标签统一到同一视觉中轴。
- 网页端中文字体改为项目内本地子集字体，只保留当前页面用到的中文字符，避免整包中文字体依赖和线上字体加载失败。
- 修复小尺寸浏览器窗口下右侧软件预览被挤到下方后无法滚动的问题，Web Preview 容器改为独立 `100dvh` 滚动视口。
- 迷你悬浮展示恢复浅色配浅色、深色配深色的正确主题逻辑；迷你模式下移除页面橙色光晕干扰，低透明度时透出中性灰黑舞台背景。
- README 首屏删去非必要状态徽章，改为更宽松的产品标题、在线体验和 Windows 下载双入口，降低顶部拥挤感。

### v0.8.5

- Web Preview 首屏继续收紧左右两栏间距，品牌区、版本胶囊、主标题、副标题、下载入口和三枚核心标签重新梳理为更简洁的产品橱窗布局。
- 网页端字体策略改为可靠的本机中文字体栈，不再依赖外部字体请求，避免字体加载失败导致标题挤压或排版错乱；字体作用域仍限定在官网文案区域。
- 修复浅色模式 Logo 外圈过白的问题，版本号展示改为更克制的胶囊样式，GitHub 按钮图标与文案间距进一步收紧。
- 迷你悬浮展示取消“透明度联动背景”方案，改为固定的浅色 / 深色展示舞台；透明度滑杆只改变迷你悬浮窗自身透明度，避免看起来像背景在变色。
- 明确 Web Preview 使用浏览器本机 localStorage 保存预览配置，不会影响其他用户访问官网时的初始体验。
- README 首屏重构为更正式的产品入口：放大核心标语，保留在线体验与 Windows 下载两个主路径，并删除容易干扰用户路径的顶部 Release 链接表达。

### v0.8.4

- 修复 Web Preview 首屏标题在宽度不足或字体回退时被挤成竖排的问题，标题两行改为固定横排设计并保留更舒展的行距。
- 网页端文案字体改为可加载的开源中文字体，并将字体作用域限制在官网文案、按钮、标签和页脚区域，避免影响软件演示窗口内部 UI。
- 收窄 GitHub 按钮图标与文案间距，让仓库入口更紧凑、清晰。
- 补充 Web Preview 回归测试，锁定标题横排、字体作用域和按钮间距，避免后续再次回退。

### v0.8.3

- Web Preview 首屏标题重排为“看见每一秒的 / 收入跳动”，第二行使用强调色与更强字号层级，副标题收敛为单行表达。
- 网页端字体系统升级：为标题、正文、按钮和标签分别设置更贴合产品气质的中文字体栈，降低默认系统字体的中规中矩感。
- 三枚核心标签重写为“秒秒入账 / 角落常驻 / 隐私安心”，内容居中并突出隐私感知，替换过于抽象的“本地保存”。
- Web Preview 迷你悬浮展示改为反向明暗：浅色网页中使用深色迷你窗，深色网页中使用浅色迷你窗，强化悬浮识别。
- 修复网页端迷你悬浮透明度调低后圆角退化成矩形的问题，预览层单独锁定圆角、边界和文字颜色，不影响 Windows 桌面端迷你窗口。
- 深色 Web Preview 背景与软件窗口重新拉开层次，通过更干净的舞台背景、边界和低噪声光感避免主窗口融进页面背景。

### v0.8.2

- Web Preview 首屏大幅精简：删除无效“开始体验”锚点，保留 Windows 下载与 GitHub 两个核心入口。
- 网页端产品橱窗新增左上角产品 Logo 与当前版本号展示。
- Web Preview 三枚核心优势短标签重写为“实时入账 / 迷你悬浮 / 本地保存”，保留简短说明但避免重复解释。
- 修复网页端进入迷你悬浮模式后仍占用主窗口尺寸的问题，迷你模式改为紧凑舞台展示。
- 修复 Web Preview 深色模式主窗口背景透出网页背景的问题，网页预览窗口改为不透明应用背景。
- README 删除单独“近期改进”章节，将必要的 Web Preview 与发布可靠性信息合并进核心特性。

### v0.8.1

- Web Preview 重构为更接近产品官网的线上橱窗：首屏强化产品主张、Windows 下载入口、在线体验入口和 GitHub 仓库入口。
- 网页端主窗口预览尺寸收敛到接近桌面端默认的 `480 × 460`，避免在线体验窗口过宽、过大导致真实桌面手感失真。
- Web Preview 页面空间、背景层次、行动按钮和核心优势说明重新排版，减少开发预览感，让主窗口成为第一视觉焦点。
- README 首屏新增 Web Preview 与 Windows 下载主入口，并补充最近版本在网页体验、视觉稳定性和工程质量上的迭代进展。

### v0.8.0

- 新增 PayDance Web Preview：通过 GitHub Pages 在线体验核心薪资计算、首次配置、设置中心和迷你悬浮效果。
- 根应用改为按运行目标加载 Desktop App 或 Web Preview，网页构建不执行 Tauri 原生窗口、托盘和 Store 调用。
- 配置存储抽象为平台适配层：Windows 桌面版继续使用 Tauri Store，Web Preview 使用浏览器 localStorage。
- 外链打开能力改为平台适配：桌面端使用 Tauri opener，网页端使用浏览器新标签页。
- Web Preview 隐藏托盘、窗口置顶、开机自启动等桌面专属能力，并在 README 中明确体验版与完整 Windows 版边界。
- 新增 GitHub Pages 自动部署 workflow，并将 Web Preview 构建纳入本地与 CI 验证链路。
- PRODUCT / DESIGN 同步补充 Web Preview 定位，同时继续明确 Windows 11 是当前正式验证平台。

### v0.7.16

- 主界面金额变化光效恢复为 v0.7.9 的自然单层 `drop-shadow` 过渡，人民币符号与金额光亮重新保持同步、克制和柔和。
- 移除 0.7.14 起新增的金额 pulse 关键帧与强光变量，避免光感出现生硬闪烁或廉价发光感。
- 保留 v0.7.13 起的自适应薪资 ticker 性能优化，不回退桌面常驻和迷你悬浮模式下的 CPU/电量改进。
- 补充金额光效回归测试，锁定“无缩放、无位移、无关键帧、使用 v0.7.9 光效参数”的视觉边界。

### v0.7.15

- 修复主窗口缩小后设置中心薪资、工作时间与午休输入区被强制折成单列的问题，恢复桌面窗口下稳定的双列表单。
- 重做设置中心底部归属区为稳定两栏布局，避免版本、作者、GitHub 与版权信息在窄窗口下全部居中换行。
- 主金额变化时的脉冲光彻底移除缩放与位移，只保留短促单层光感，避免金额本体出现变大变小的跳动感。
- 首次启动向导与设置中心共享输入数字字重，统一金额、时间与午休输入的数字显示质感。
- 补充设置中心窄窗口、归属区、首次向导输入字重和金额 pulse 无缩放的回归测试。

### v0.7.14

- 主界面金额脉冲光改为短促单层关键帧：光晕范围更容易被感知，持续时间仍保持 220ms，并继续尊重系统减少动态效果偏好。
- 今日进度条填充层改为完整圆角层从右侧裁切，修复左侧起点不够圆润的问题，同时保留圆点的 transform 位移。
- 全局键盘焦点样式不再直接套到普通输入框，避免数字输入时出现橙色竖线；金额、时间、午休输入框改用本地克制焦点环。
- 补充进度条结构、主金额脉冲、全局焦点和共享字段焦点的回归测试，锁住 0.7.9 之后反复出现的视觉副作用。

### v0.7.13

- 薪资实时 ticker 从每帧 `requestAnimationFrame` 业务重算改为按金额小数变化和状态边界自适应调度，降低桌面常驻与迷你悬浮模式下的 CPU/电量负担。
- 设置持久化拆分薪资配置与 UI/窗口偏好保存：薪资输入临时无效时不再阻断主题、窗口尺寸、迷你透明度和首次启动状态保存。
- 看板两段竖向分隔线统一为稳定粗细，进度条改用 transform 驱动填充和圆点位移，减少布局重排并保持现有视觉方向。
- 本地验证脚本拆为 `verify:fast` 与 `verify:release`，发布级验证补齐版本一致性、Release 锁、生产依赖审计、Rust fmt/check/clippy。
- 迷你悬浮窗口补充 Space 键恢复完整窗口，全局交互控件增加 `:focus-visible` 焦点样式，改善键盘可访问性。
- 托盘事件名、设置 Store key 与窗口状态保存 debounce 收敛为命名常量，降低字符串拼写和魔法数字维护风险。
- 在 PRODUCT 中明确 Windows 11 是当前正式验证平台。

### v0.7.12

- 工程检查脚本收敛到 Node canonical，Release notes 从 CHANGELOG 读取的流程不再依赖 PowerShell，保留 Windows 专属环境脚本。
- 新增 ESLint、Vue a11y lint 与 Prettier 检查，并接入本地验证、CI 与 Release workflow，减少模板语义、格式和无障碍回归。
- 设置中心与首次启动向导抽出共用 SegmentedControl、SwitchRow、SettingsGroup，并将向导三步拆为独立组件，保持视觉不变但降低维护成本。
- 薪资计算模型拆成配置、时间、校验、工作段、夜班和快照模块，`src/lib/salary.ts` 保留兼容出口，核心薪资测试继续覆盖原路径。
- 设置持久化新增写入前配置校验和写后回读核对，避免无效配置或 Store 静默异常污染本地配置。
- 改善薪资模式、工作日、主金额、迷你窗口和透明度滑杆的可访问性语义，并增加文件体积边界测试防止大组件再次膨胀。
- Tauri installer bundle target 从泛化 `all` 收窄为明确 Windows 目标；本版不引入付费代码签名，继续使用 SHA256 校验发布可信度。

### v0.7.11

- App 主文件瘦身为顶层接线层，主看板、窗口弹层、页面级视图状态、迷你拖拽与金额格式化拆出独立模块，降低后续迭代碰撞面。
- 薪资实时 tick 改为显式单调时钟模型，避免系统授时跳变或休眠恢复造成今日入账倒退与瞬跳，并补充单调时钟回归测试。
- 窗口状态持久化加入 latest-only 保存队列，处理托盘、窗口尺寸、迷你模式与透明度同帧写入时的竞态风险。
- 主题切换遮罩改为轻量模糊过渡，切换期间暂停金额脉冲，避免纯色遮罩盖住跳动数字。
- 看板竖向分隔线可见度略微增强，深色模式数字输入框跟随深色原生控件配色；通用品牌与敏感信息检查改为 Node 脚本。

### v0.7.10

- 设置中心与首次启动向导复用薪资模式、薪资输入、工作日、上下班时间与午休控件，减少后续双线维护成本，同时保持现有视觉与交互。
- 配置保存失败时不再向外抛出异常，避免磁盘或 Store 临时不可写导致应用状态被打断。
- 抽离浅色 / 深色主题 token 到独立样式文件，App 主文件更聚焦页面编排；深色模式表面、看板与首次向导遮罩进一步收敛为更干净的黑色层次。
- 设置中心与首次启动向导间距略微放松，降低表单拥挤感，并补充共享控件与配置保存容错的行为测试。

### v0.7.9

- 修复 0.7.2 起暗色模式下“薪资说明”面板卡片被浅色背景覆盖的问题，恢复随主题变化的面板背景与边框。
- 薪资说明金额颜色恢复为主题文本色，暗色模式保持清晰可读，浅色模式保持高对比。
- 补充薪资说明面板主题化回归测试，避免暗色卡片再次退化为灰白面板。

### v0.7.8

- 透明度面板右侧百分比改为与“透明度”标题同色、同字重，减少视觉层级误判。
- 补充高 DPI 与非零屏幕工作区下的迷你透明度面板定位回归测试，约束居中对齐误差。
- README 重整为产品橱窗，强化核心体验、下载校验、隐私边界与工程质量表达。

### v0.7.7

- 修正透明度面板仍可能没有与迷你悬浮窗口居中对齐的问题，定位锚点改为用户实际看到的窗口内容区。
- 打开透明度面板前会重置并读取面板内容区尺寸，再反推外层窗口坐标，减少透明窗口边框偏移。
- 补充内容区对齐回归测试，覆盖 Windows 无边框透明窗口可能存在不可见外框的场景。

### v0.7.6

- 修复迷你悬浮窗口右键无法弹出透明度面板的问题，右键事件现在会同步传递视觉锚点。
- 透明度面板定位改回 Tauri 原生窗口几何，使用物理像素计算，适配 Windows DPI 缩放。
- 增加兜底定位与行为测试，避免窗口 API 读取失败时出现“点了没反应”。

### v0.7.5

- 优化迷你悬浮透明度面板与窗口本体的居中对齐，右键点击位置只负责触发，不决定面板中心。
- 透明度面板定位改用浏览器逻辑像素，避免物理像素、缩放比例和鼠标坐标混用。
- 为不同右键点击位置补充回归测试，保证面板在上方或下方显示时都保持中心对齐。

### v0.7.4

- 修复 v0.7.3 迷你悬浮窗口右键无法弹出透明度面板的问题，补齐所需 Tauri 窗口读取权限。
- 透明度面板打开逻辑增加鼠标附近兜底路径，避免窗口几何读取失败时静默无响应。
- 透明度面板继续压缩尺寸，减少浮层存在感，同时保留细滑轨和小圆点的可操作性。

### v0.7.3

- 迷你悬浮透明度面板重做为小型浮层，滑轨和圆点收窄，避免横向大面板带来的生硬感。
- 透明度面板改为跟随迷你悬浮窗口居中显示，空间不足时自动切换到上方并限制在屏幕工作区内。
- 今日入账金额脉冲光改为单层光晕，范围比 v0.7.1 更容易感知，持续时间保持克制。

### v0.7.2

- 配置加载加入故障恢复边界；若本地配置损坏或 Store 读取失败，应用会回退默认配置并继续可用。
- 迷你悬浮窗口右键透明度面板大幅缩小，并使用自定义细滑轨与主题专属明暗质感。
- 薪资说明面板拆成独立组件，金额数字加重，并补充 Store 故障恢复、透明度面板交互与金额脉冲测试。

### v0.7.1

- 迷你悬浮窗口新增右键透明度面板，可在 `10% - 100%` 之间拖动调节。
- 透明度配置写入本地设置，下次启动沿用上次透明度，同时保留单层胶囊视觉。
- 新增 PowerShell 7 与 Git 中文显示编码修复脚本，并补充 `.editorconfig` 统一源码 UTF-8 规则。

### v0.7.0

- 以本地 v0.6.9 为稳定基线重做窗口边界处理，解决深色模式四角多余背景块问题。
- 主界面与迷你悬浮窗口禁用无意义的 WebView 右键菜单；托盘左键直接唤起主界面，右键保留菜单。
- 设置中心与薪资说明打开后仍可拖拽移动主窗口，首次启动向导提高不透明度并统一数字字体。
- 去除迷你悬浮窗口外围装饰性阴影，保留稳定单层胶囊视觉；CI 与 Release workflow 增加 Rust 构建缓存。

### v0.6.9

- 修复深色模式切换后四角出现双层圆角暗边的问题，窗口主题底色与圆角收敛到同一层承载。
- 看板 `h`、`m`、`¥` 的字号与间距继续优化，今日预计的 `¥` 与金额数字等高。
- 薪资说明弹窗和设置中心中的数字、英文与符号统一使用看板字体。

### v0.6.8

- 主窗口移除外层浅灰边距后，标题栏状态区与窗口操作区整体向内、向下调整。
- 深色模式看板去除顶部泛白高光，改为更干净的深色层次、细边框和柔和阴影。
- 今日进度条保留平面轨道，同时恢复 v0.6.6 深色模式下的三段金色填充质感。

### v0.6.7

- 新增桌面端单实例保护，重复启动时聚焦已有主窗口，避免双开后主题状态不同步。
- 主题切换保留统一入口、点击锁和乱序保护，但移除全局视觉 transition 冻结。
- 主界面看板回到更平面的表达，深色模式取消厚重玻璃光晕与凹槽轨道。

### v0.6.6

- 修复浅色 / 深色模式快速切换时前端主题与原生窗口主题偶发不同步的问题。
- 深色模式“薪资说明”入口 Hover 同步升级为深色玻璃底、低透明橙色描边和柔和光晕。
- 深色主界面看板改用更深的墨色玻璃面、细高光边和更克制的阴影。

### v0.6.5

- 左上角状态圆点与状态文案改为与右侧窗口操控图标保持同一视觉中线。
- 看板“今日预计”中的 `¥` 调整为与 `h / m` 单位一致的浅灰色。
- 看板数字字体切换为更贴近 Windows 11 Fluent 气质的 `Segoe UI Variable Display` 优先栈。

### v0.6.4

- 修复今日进度条右侧圆点被轨道裁切的问题，圆点完整浮在进度轨道上。
- 看板“已工作”和“距离下班”时长回到 `h / m` 表达。
- 主界面金额禁用千分位分隔，`1500.00` 不再显示为 `1,500.00`。

### v0.6.3

- 夜班状态文案改为基于当前实际工作段判断，进入 22:00 后的工作段才显示“正在夜班”。
- 首次启动向导“工作时间”步骤中，只有勾选“剔除午休”后才展示午休时间输入。
- 设置中心“开机自动启动”开关与午休“剔除”开关做右侧对齐，减少视觉偏移。

### v0.6.2

- 看板数字时间格式继续精修，冒号左右增加细微间距，时长扫读更轻松。
- 今日预计金额中的 `¥` 与后续金额数字完全等高，统一金额视觉重心。
- 首次启动向导第三步由“外观风格”改为“使用偏好”，并新增“开机自动启动”选择。

### v0.6.1

- 移除 v0.6.0 主界面金额下方的“每秒 / 每分钟”常驻小字，让金额重新成为唯一视觉焦点。
- 金额小数取消金色强调，恢复与整数一致的主文字色。
- 保留整体看板方向，并将今日进度百分比改为悬停提示，减少常驻信息密度。

### v0.6.0

- 主界面升级为“薪资脉搏看板”，让“今日已入账”金额成为更明确的第一视觉焦点。
- 底部统计区从三张独立卡片优化为一组整体信息，工作时长、动态状态指标和今日预计收入更紧凑。
- 本版不改动迷你悬浮模式，不新增快捷键、提醒或分段时间轴。

### v0.5.17

- 修复跨零点但不属于夜班的状态文案，避免 `09:30 - 00:30` 这类班次误显示“正在夜班”。
- 设置中心新增“启动”小卡片，可开启或关闭“开机自动启动”。
- 接入 Tauri autostart 插件，并将权限收紧到启用、禁用和读取自启动状态三项。

### v0.5.16

- 主界面第二张统计卡片改为按状态动态表达：距离上班、距离下班、距离复工、今日完成或休息态。
- 夜班跨零点计算继续加固，覆盖工作日夜班跨入休息日凌晨、连续夜班等边界。
- 新增旧品牌文案扫描和敏感信息扫描脚本，并接入本地验证、CI 与 Release workflow。

### v0.5.15

- 修复连续工作日夜班在次日下班后、下一班尚未开始时的归属边界问题。
- 设置中心保持 v0.5.10 小卡片结构，仅微调底部作者信息和 GitHub 仓库入口。
- 依赖升级到当前可用版本，Release workflow 增加生产依赖审计步骤。

### v0.5.14

- 设置中心整体回退到 v0.5.10 的独立小卡片效果，恢复轻量布局。
- 保留夜班跨零点支持，并修复跨零点班次在次日下班后过早显示“今日休息”的边界问题。
- 保留版本号、作者 Mr.Baoboer、GitHub 仓库入口和居中版权信息，但移除“关于”标题。

### v0.5.10

- 将营销主视觉替换为新的 PayDance 产品海报。
- README 界面体验区聚焦“今日入账看板”和“首次配置三步上手”两张无版本号海报。
- 设置中心底部作者归属区新增当前版本号展示，Release workflow 支持手动填写标签触发正式发布。

### v0.5.9

- README 产品页新增界面预览区，展示今日入账看板、收入粒度换算和迷你悬浮模式。
- 数字输入解析改为共用逻辑，清空薪资或工作天数输入框时不再把配置误写成 `0`。
- Tauri 增加 CSP，并将窗口、事件、Store 权限收紧到当前实际使用范围。

### v0.5.8

- README 增加主视觉海报、SHA256 校验示例、首次配置重置命令和本地环境修复说明。
- 主窗口尺寸会随用户调整持久化，退出迷你悬浮模式后恢复用户自己的主窗口大小。
- GitHub Actions 增加版本一致性检查、生产依赖审计、并发控制和基于 README 的 Release notes 生成。

### v0.5.7

- 设置中心底部新增作者署名 `Mr.Baoboer`。
- 新增 GitHub 仓库入口按钮，点击后通过系统默认浏览器打开项目仓库。
- 接入 Tauri opener 插件，外部链接不再占用应用 WebView。

### v0.5.6

- README 重构为面向用户的产品主页，强化快速下载、产品亮点、隐私说明、跨平台边界和开发发布流程。
- Release workflow 增加 SHA256 校验文件产物，便于下载后核对正式版附件。

### v0.5.5

- 英文名正式调整为 PayDance，中文名保持“薪跳”。
- 产品定位统一为“桌面实时工资看板”。
- 清理旧英文品牌、历史项目名和旧营销表述，GitHub 仓库同步更名为 `PayDance`。

### v0.5.3

- 回退首次启动向导第一步数字输入框的居中对齐，恢复为更自然的左侧输入。
- 将薪资与工作天数计量单位同步到设置中心，月薪、日薪、时薪显示“元”，每月工作天数显示“天”。
- 将 v0.5.3 作为当时最新正式版发布，并移除原 v0.5.2 GitHub Release。

### v0.5.0

- 新增月薪、日薪、时薪三种薪资输入模式，并自动换算日薪、时薪、分薪和秒薪。
- 新增每周工作日设置，非工作日显示“今日休息”并停止当日收入累计。
- 新增首次启动向导，主窗口排版、金额数字和界面字号改为随窗口尺寸平滑自适应。

### v0.4.1

- 完成产品命名与说明文案整理。
- 主界面 `¥ + 金额数字` 组合整体水平居中，并优化符号与数字间距。
- README 全面更新，补充仓库地址、设计方向、功能说明和版本演进。

### v0.2.0

- 增加核心薪资逻辑单元测试、配置校验提示，以及 GitHub CI 与 Release 自动构建流程。
- 优化迷你悬浮模式，支持拖拽移动、双击恢复主窗口，并记忆迷你窗口尺寸。
- 修复关闭午休后异常午休字段仍影响薪资逻辑的问题，以及迷你模式窗口最小尺寸受主窗口约束的问题。

### v0.1.0

- 首个公开发行版，包含 Windows 11 风格实时工资看板、薪资与作息配置、实时收入展示、迷你悬浮、系统托盘、窗口置顶和亮色 / 暗色主题。
- 修复 Release 页面中文介绍显示为问号的问题。
- 修复 Windows EXE 启动时额外弹出黑色终端窗口的问题。
